// ejemplo.js

// Función 1: suma dos números
function suma(a, b) {
  return a + b;
}

// Función 2: verifica si un número es par
function esPar(n) {
  return n % 2 === 0;
}

module.exports = { suma, esPar };

